# Optional. With a key, the service uses Claude; without it, a deterministic fallback
# runs (so tests/evals work with no secrets).
ANTHROPIC_API_KEY=

# Most capable: claude-opus-4-8  |  balanced default: claude-sonnet-4-6
MODEL=claude-sonnet-4-6
